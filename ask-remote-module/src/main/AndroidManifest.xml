<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.androidstarterkit.module">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SampleActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".sample.activity.ScrollViewActivity" android:label="ScrollView"/>
        <activity android:name=".sample.activity.GridViewActivity" android:label="GridView"/>
        <activity android:name=".sample.activity.RecyclerViewActivity" android:label="RecyclerView"/>
        <activity android:name=".sample.activity.ListViewActivity" android:label="ListView"/>
        <activity android:name=".sample.activity.SlidingTabActivity" android:label="SlidingTabLayout"/>
        <activity android:name=".sample.activity.SlidingIconTabActivity" android:label="SlidingIconTabLayout"/>
    </application>

</manifest>